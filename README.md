# Floating Spheres

A canvas animation of transparent bubbles drifting and bouncing off each other and the screen edges. Spheres are rendered in three depth layers — each with its own size, speed, and opacity — while continuous collision detection keeps fast-moving circles from tunneling through walls or each other.
